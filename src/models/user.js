export default class User {
    constructor(id, firstName, lastName, city, age) {//oluşturucu
        // console.log("user oluştu")
        this.id = id
        this.firstName = firstName
        this.lastName = lastName
        this.city = city
        this.age= age
    }
}